<UserControl x:Class="BoboTech.EncyclopaediaMetallumViewer.Fragments.Footer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="400">
    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Status:"/>
            <TextBlock Grid.Column="1" Text="{Binding EMApiCallStatus}"/>
            <TextBlock Grid.Column="2" Text="Code:"/>
            <TextBlock Grid.Column="3" Text="{Binding EMApiCallCode}"/>
            <TextBlock Grid.Row="1" Text="Message:"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding EMApiCallMessage}"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text="Hash:"/>
            <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding EMApiCallHash}"/>
        </Grid>
        <TextBlock>
            <Hyperlink Command="{Binding OpenEMApiDonationUrlCommand}">
                <TextBlock Text="{Binding EMApiDonationMessage}" Margin="0"/>
            </Hyperlink>
        </TextBlock>
        <StackPanel Orientation="Horizontal" Name="ButtonsPanel"/>
    </StackPanel>
</UserControl>